<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Houses Page</title>
</head>
<body>
	<h1> <%=house%>! </h1>
	<h3>Roster:</h3>
	<ul>
		<%for(id in students){ 
			if(students[id].house === house){ %>

			<li> <a href="/student/<%=id%>"> <%=students[id].name %> </a>
			</li>
			<% } %>
		<% } %>
	</ul>
</body>
</html>